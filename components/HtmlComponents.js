import styled from "styled-components";

const PTag = styled.p`
  font-style: normal;
  font-weight: 400;
  font-size: 20px;
  line-height: 27px;
`;

const PTagBold = styled(PTag)`
  font-weight: 700;
`;

export { PTag, PTagBold };
